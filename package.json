{
  "name": "todos-view",
  "version": "1.0.0",
  "description": "view of todos app using onsen ui",
  "main": "index.js",
  "author": "duongtdn",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf lib",
    "babel": "babel src -d lib",
    "build": "npm run clean && npm run babel",
    "prepublish": "npm run build",
    "compile": "npm run build && browserify lib/ -o bundle.js && echo Compile finished"
  },
  "dependencies": {
    "onsenui": "^2.0.4",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-onsenui": "^1.0.3",
    "react-redux": "^4.4.6",
    "redux": "^3.6.0"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.13.2",
    "babel-preset-react": "^6.11.1",
    "babel-plugin-transform-object-rest-spread": "^6.16.0"
  }
}
